$(document).ready(function()
{

	var sameHeightElements = [];
	/*

	var heights = $(".officerTitle").map(function()
	{
		return $(this).height();
	}).get(),

	maxHeight = Math.max.apply(null, heights);


	$(".officerTitle").height(maxHeight);

	var heights = $(".homePageSection").map(function()
	{
		return $(this).height();
	}).get(),

	maxHeight = Math.max.apply(null, heights);

	$(".homePageSection").height(maxHeight);
	*/

});
